[application]
name = VoiceInk

# Only the preferences plist. The Application Support dir holds Whisper
# models + recordings (~hundreds of MB) and is intentionally not synced.
[configuration_files]
Library/Preferences/com.prakashjoshipax.VoiceInk.plist
