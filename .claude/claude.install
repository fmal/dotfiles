#!/usr/bin/env zsh

if command -v claude &> /dev/null; then
  echo "Claude Code is already installed: skipping install."
  exit 0
fi

curl -fsSL https://claude.ai/install.sh | bash

# claude config set -g theme dark-daltonized 2>/dev/null
# claude config set -g autoConnectIde true 2>/dev/null
# claude config set -g diffTool auto 2>/dev/null
